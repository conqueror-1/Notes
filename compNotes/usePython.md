# Efficiently use Python in Competitive Programming

* reassignment using None

```py
#decrementations
for i in range (5,1,-1):
	print(i)
```
* maths

```py
import math
math.fmod(-10,3)
math.floor(3/2)
math.cel(3/2)
float("inf")
float("-inf")
```

* arrays

```py
arr.append()
arr.pop()
arr.insert()

# array initialization
n=5 
arr=[0]*n

for i in range (len(arr)):
	print(nums[i])

for i,n in enumerate(arr):
	print(i,n)

for n1,n2 in zip(num,num2):
	print(n1,n2)

arr.sort()
arr.sort(reverse=true)

arr.sort(key=lambda x:len(x))

arr =[i for i in range(4)]

# strings

s = "abc"
s+="def"

#ascii valuse
ord("a")

join function
```

```
from collections import deque

queue = deque()
queue.append()
queue.popleft()

mySet = set ()
mySet.add(1)
len(mySet)
print(1 in mySet)
mySet = {i for i in range(5)}

myMap = {}
myMap["alice"] = 88
myMap.pop('alice')
myMap = {"alice":90,"bob":70}

maymap = {i:2*i for i in range(3)}
for key in myMap:
	print (key,myMap[key])
for key,value in myMap.items{}:
	print(key,value)
```
```
import heapq
heapq.jeappush(minHeap,3)
```
	
